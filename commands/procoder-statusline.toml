description = "Install, inspect, or remove procoder's statusline badge in Claude Code."
prompt = """
Use the procoder-statusline skill. Arguments (optional: install, status,
uninstall): $ARGUMENTS

Run `statusline status` before anything else and report what it says. If a
statusLine that is not procoder's is configured, show what is there and what
procoder would set, and ask before replacing it.
"""
